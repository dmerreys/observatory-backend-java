diff --git a/src/main/java/com/observatorio/backend_ia/config/SecurityConfig.java b/src/main/java/com/observatorio/backend_ia/config/SecurityConfig.java
index 60d271c..6ef8bb3 100644
--- a/src/main/java/com/observatorio/backend_ia/config/SecurityConfig.java
+++ b/src/main/java/com/observatorio/backend_ia/config/SecurityConfig.java
@@ -7,6 +7,7 @@ import org.springframework.security.authentication.AuthenticationManager;
 import org.springframework.security.config.annotation.authentication.configuration.AuthenticationConfiguration;
 import org.springframework.security.config.annotation.web.builders.HttpSecurity;
 import org.springframework.security.config.annotation.web.configuration.EnableWebSecurity;
+import org.springframework.security.config.annotation.method.configuration.EnableMethodSecurity;
 import org.springframework.security.config.annotation.web.configurers.AbstractHttpConfigurer;
 import org.springframework.security.config.http.SessionCreationPolicy;
 import org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder;
@@ -19,6 +20,7 @@ import org.springframework.web.cors.UrlBasedCorsConfigurationSource;
 
 @Configuration
 @EnableWebSecurity
+@EnableMethodSecurity
 public class SecurityConfig {
 
     private final JwtAuthenticationFilter jwtAuthenticationFilter;
diff --git a/src/main/java/com/observatorio/backend_ia/controller/ResourceController.java b/src/main/java/com/observatorio/backend_ia/controller/ResourceController.java
new file mode 100644
index 0000000..f0191c1
--- /dev/null
+++ b/src/main/java/com/observatorio/backend_ia/controller/ResourceController.java
@@ -0,0 +1,74 @@
+// controller/ResourceController.java
+package com.observatorio.backend_ia.controller;
+
+import com.observatorio.backend_ia.commons.api.GenericResponse;
+import com.observatorio.backend_ia.controller.dto.ResourceRequest;
+import com.observatorio.backend_ia.controller.dto.ResourceResponse;
+import com.observatorio.backend_ia.model.Resource;
+import com.observatorio.backend_ia.repository.ResourceRepository;
+import lombok.RequiredArgsConstructor;
+import org.springframework.http.ResponseEntity;
+import org.springframework.security.access.prepost.PreAuthorize;
+import org.springframework.web.bind.annotation.*;
+
+import java.util.List;
+import java.util.stream.Collectors;
+
+@RestController
+@RequestMapping("/api/resources")
+@RequiredArgsConstructor
+public class ResourceController {
+
+    private final ResourceRepository resourceRepository;
+
+    @GetMapping
+    public ResponseEntity<GenericResponse<List<ResourceResponse>>> getAll() {
+        List<Resource> resources = resourceRepository.findAllByOrderByCreatedAtDesc();
+        List<ResourceResponse> dtos = resources.stream().map(this::toResponse).collect(Collectors.toList());
+        return ResponseEntity.ok(GenericResponse.createSuccessResponse(dtos));
+    }
+
+    @GetMapping("/type/{type}")
+    public ResponseEntity<GenericResponse<List<ResourceResponse>>> getByType(@PathVariable String type) {
+        List<Resource> resources = resourceRepository.findByType(type);
+        List<ResourceResponse> dtos = resources.stream().map(this::toResponse).collect(Collectors.toList());
+        return ResponseEntity.ok(GenericResponse.createSuccessResponse(dtos));
+    }
+
+    @PostMapping
+    @PreAuthorize("hasRole('ADMIN')")
+    public ResponseEntity<GenericResponse<ResourceResponse>> create(@RequestBody ResourceRequest request) {
+        Resource resource = new Resource();
+        resource.setTitle(request.getTitle());
+        resource.setDescription(request.getDescription());
+        resource.setType(request.getType());
+        resource.setUrl(request.getUrl());
+        resource.setSource(request.getSource());
+
+        Resource saved = resourceRepository.save(resource);
+        return ResponseEntity.ok(GenericResponse.createSuccessResponse(toResponse(saved)));
+    }
+
+    @DeleteMapping("/{id}")
+    @PreAuthorize("hasRole('ADMIN')")
+    public ResponseEntity<GenericResponse<Void>> delete(@PathVariable Long id) {
+        if (!resourceRepository.existsById(id)) {
+            return ResponseEntity.badRequest()
+                    .body(GenericResponse.createErrorResponse("Recurso no encontrado"));
+        }
+        resourceRepository.deleteById(id);
+        return ResponseEntity.ok(GenericResponse.createSuccessResponse());
+    }
+
+    private ResourceResponse toResponse(Resource r) {
+        ResourceResponse dto = new ResourceResponse();
+        dto.setId(r.getId());
+        dto.setTitle(r.getTitle());
+        dto.setDescription(r.getDescription());
+        dto.setType(r.getType());
+        dto.setUrl(r.getUrl());
+        dto.setSource(r.getSource());
+        dto.setCreatedAt(r.getCreatedAt());
+        return dto;
+    }
+}
\ No newline at end of file
diff --git a/src/main/java/com/observatorio/backend_ia/controller/dto/ResourceRequest.java b/src/main/java/com/observatorio/backend_ia/controller/dto/ResourceRequest.java
new file mode 100644
index 0000000..890dabf
--- /dev/null
+++ b/src/main/java/com/observatorio/backend_ia/controller/dto/ResourceRequest.java
@@ -0,0 +1,21 @@
+// controller/dto/ResourceRequest.java
+package com.observatorio.backend_ia.controller.dto;
+
+import jakarta.validation.constraints.NotBlank;
+import lombok.Data;
+
+@Data
+public class ResourceRequest {
+    @NotBlank
+    private String title;
+
+    private String description;
+
+    @NotBlank
+    private String type; // video, pdf, link
+
+    @NotBlank
+    private String url;
+
+    private String source;
+}
\ No newline at end of file
diff --git a/src/main/java/com/observatorio/backend_ia/controller/dto/ResourceResponse.java b/src/main/java/com/observatorio/backend_ia/controller/dto/ResourceResponse.java
new file mode 100644
index 0000000..5194e65
--- /dev/null
+++ b/src/main/java/com/observatorio/backend_ia/controller/dto/ResourceResponse.java
@@ -0,0 +1,16 @@
+// controller/dto/ResourceResponse.java
+package com.observatorio.backend_ia.controller.dto;
+
+import lombok.Data;
+import java.time.LocalDateTime;
+
+@Data
+public class ResourceResponse {
+    private Long id;
+    private String title;
+    private String description;
+    private String type;
+    private String url;
+    private String source;
+    private LocalDateTime createdAt;
+}
\ No newline at end of file
diff --git a/src/main/java/com/observatorio/backend_ia/model/Resource.java b/src/main/java/com/observatorio/backend_ia/model/Resource.java
new file mode 100644
index 0000000..30400ad
--- /dev/null
+++ b/src/main/java/com/observatorio/backend_ia/model/Resource.java
@@ -0,0 +1,35 @@
+package com.observatorio.backend_ia.model;
+
+import jakarta.persistence.*;
+import lombok.Data;
+import java.time.LocalDateTime;
+
+@Entity
+@Table(name = "resources")
+@Data
+public class Resource {
+
+    @Id
+    @GeneratedValue(strategy = GenerationType.IDENTITY)
+    private Long id;
+
+    @Column(nullable = false, length = 300)
+    private String title;
+
+    @Column(columnDefinition = "TEXT")
+    private String description;
+
+    @Column(nullable = false)
+    private String type; // "video", "pdf", "link"
+
+    @Column(nullable = false)
+    private String url; // YouTube embed, PDF link, o página web
+
+    @Column(length = 200)
+    private String source;
+
+    @Column(nullable = false)
+    private LocalDateTime createdAt = LocalDateTime.now();
+
+    private LocalDateTime updatedAt;
+}
\ No newline at end of file
diff --git a/src/main/java/com/observatorio/backend_ia/repository/ResourceRepository.java b/src/main/java/com/observatorio/backend_ia/repository/ResourceRepository.java
new file mode 100644
index 0000000..31cf781
--- /dev/null
+++ b/src/main/java/com/observatorio/backend_ia/repository/ResourceRepository.java
@@ -0,0 +1,11 @@
+// repository/ResourceRepository.java
+package com.observatorio.backend_ia.repository;
+
+import com.observatorio.backend_ia.model.Resource;
+import org.springframework.data.jpa.repository.JpaRepository;
+import java.util.List;
+
+public interface ResourceRepository extends JpaRepository<Resource, Long> {
+    List<Resource> findByType(String type);
+    List<Resource> findAllByOrderByCreatedAtDesc();
+}
\ No newline at end of file
diff --git a/src/main/java/com/observatorio/backend_ia/security/JwtAuthenticationFilter.java b/src/main/java/com/observatorio/backend_ia/security/JwtAuthenticationFilter.java
index b6e8993..343701f 100644
--- a/src/main/java/com/observatorio/backend_ia/security/JwtAuthenticationFilter.java
+++ b/src/main/java/com/observatorio/backend_ia/security/JwtAuthenticationFilter.java
@@ -36,17 +36,22 @@ public class JwtAuthenticationFilter extends OncePerRequestFilter {
         }
 
         String token = authHeader.substring(7);
-        String username = jwtUtil.extractUsername(token);
 
-        if (username != null && SecurityContextHolder.getContext().getAuthentication() == null) {
-            UserDetails userDetails = userDetailsService.loadUserByUsername(username);
+        try {
+            String username = jwtUtil.extractUsername(token);
 
-            if (jwtUtil.validateToken(token)) {
-                UsernamePasswordAuthenticationToken authToken = new UsernamePasswordAuthenticationToken(
-                        userDetails, null, userDetails.getAuthorities());
-                authToken.setDetails(new WebAuthenticationDetailsSource().buildDetails(request));
-                SecurityContextHolder.getContext().setAuthentication(authToken);
+            if (username != null && SecurityContextHolder.getContext().getAuthentication() == null) {
+                UserDetails userDetails = userDetailsService.loadUserByUsername(username);
+
+                if (jwtUtil.validateToken(token)) {
+                    UsernamePasswordAuthenticationToken authToken = new UsernamePasswordAuthenticationToken(
+                            userDetails, null, userDetails.getAuthorities());
+                    authToken.setDetails(new WebAuthenticationDetailsSource().buildDetails(request));
+                    SecurityContextHolder.getContext().setAuthentication(authToken);
+                }
             }
+        } catch (Exception e) {
+            // Token inválido o expirado — sigue la cadena sin autenticación
         }
 
         filterChain.doFilter(request, response);
diff --git a/src/main/resources/application.properties b/src/main/resources/application.properties
index 6dabd1b..5cda574 100644
--- a/src/main/resources/application.properties
+++ b/src/main/resources/application.properties
@@ -6,7 +6,7 @@ spring.application.name=BackendIA
 spring.datasource.url=${SPRING_DATASOURCE_URL}
 spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
 spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
-spring.datasource.driver-class-name=org.postgresql.Driver
+#spring.datasource.driver-class-name=org.postgresql.Driver
 
 
 # JPA / Hibernate
@@ -30,6 +30,7 @@ server.error.include-message=always
 
 # Python micro URL
 python.service.url=${PYTHON_SERVICE_URL}
+#python.service.url=https://document-collector.vercel.app/
 
 # SendGrid API Key (set as environment variable)
 sendgrid.api.key=${SENDGRID_API_KEY}
@@ -48,6 +49,7 @@ resend.api.key=${RESEND_API_KEY}
 
 # destinatario por defecto
 app.default.recipient=${DEFAULT_RECIPIENT_EMAIL}
+#app.default.recipient=cristru8@gmail.com
 app.email.provider=resend
 app.mail.from=Observatorio IA <no-reply@resend.dev>
 
diff --git a/src/main/resources/db/migration/V2__create_resources.sql b/src/main/resources/db/migration/V2__create_resources.sql
new file mode 100644
index 0000000..beedc5e
--- /dev/null
+++ b/src/main/resources/db/migration/V2__create_resources.sql
@@ -0,0 +1,10 @@
+CREATE TABLE IF NOT EXISTS resources (
+    id BIGSERIAL PRIMARY KEY,
+    title VARCHAR(300) NOT NULL,
+    description TEXT,
+    type VARCHAR(50) NOT NULL,
+    url VARCHAR(500) NOT NULL,
+    source VARCHAR(200),
+    created_at TIMESTAMP NOT NULL DEFAULT NOW(),
+    updated_at TIMESTAMP
+);
